import { LightningElement } from 'lwc';

export default class TestingGit extends LightningElement {

    connectedCallback()
    {
        console.log('This is conencted callback is loading now raja');
    }
}